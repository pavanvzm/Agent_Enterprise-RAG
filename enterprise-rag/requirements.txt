# Core
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# Vector DB + LangChain
chromadb>=0.5
langchain-core>=0.2
langchain-text-splitters>=0.2

# Auth
PyJWT[crypto]>=2.8

# HTTP + uploads + YAML front matter
httpx>=0.27
python-multipart>=0.0.9
pyyaml>=6.0

# Optional (recommended): real local embeddings instead of the hashed fallback
# sentence-transformers>=2.7

# Tests
pytest>=8.0
